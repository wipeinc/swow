# -- immutable: string

module Swow
	module Constants
		REGIONS = %w(us eu kr tw)
		CHARACTER_FIELDS = %w(acheivements appearance feed guild hunterPets items
												  mounts pets petSlots professions pvp quests reputation
												  statistics stats talents titles audit)
	end
end
